// default database to be used for the system
export const LOCAL_DATABASE = 'sqlite.db';

// list of countries for which in-built mock data is available
export const localeSupportList = ['US', 'BD'];
